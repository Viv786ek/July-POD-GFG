 char firstRep (string s)
    {
        //code here.
        for(int i = 0; i < s.size() - 1; i++){
            if(s.find(s[i], i+1) != std::string::npos) return s[i];
        }
        return '#';
    }
